 cd Add-broker-time-context-1.0.0
> zip -r ../Add_time_context-1.0.0.module .

docker compose up -d --pull always --force-recreate

tip:
https://github.com/hivemq/Orange_Workshop


openssl x509 -in cert.pem -noout -text 

openssl x509 -noout -subject -ext subjectAltName -in mqtt-client-cert.pem   